from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='I built a user-based collaborative filtering recommender model for Amazon's electronic products. The models were trained, tested, and evaluated against 3 other recommender models.',
    author='Kaneesha Dawood',
    license='',
)
